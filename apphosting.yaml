kind: "AppHostingYaml"
schemaVersion: "v1"

buildConfig:
  # 確保編譯指令正確執行
  buildCommand: "npm install && npm install --prefix client && CI=false npm run build --prefix client"
  # 關鍵：明確宣告產出目錄，這樣 Firebase 才會把它搬進 Runtime 容器
  outputDirectory: "client/build"

runConfig:
  cpu: 1
  memoryMiB: 1024

buildConfig:
  buildCommand: |
    npm install
    npm install --prefix client
    cd client && CI=false npm run build && cd ..
    # 不要 mkdir web_dist 了，直接用現有的或是把 client/build 內容硬塞
    mkdir -p public_html
    cp -r client/build/* public_html/
    echo "--- 檢查 public_html ---"
    ls public_html

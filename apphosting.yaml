kind: "AppHostingYaml"
schemaVersion: "v1"

buildConfig:
  # 1. 強制進入 client 並執行 build
  buildCommand: "npm install && npm install --prefix client && CI=false npm run build --prefix client && mkdir -p ./public && cp -r client/build/* ./public/"

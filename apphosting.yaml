kind: "AppHostingYaml"
schemaVersion: "v1"

buildConfig:
  # 1. 確保編譯成功
  # 2. 移除 outputDirectory (有時它會干擾手動路徑)
  buildCommand: "npm install && npm install --prefix client && CI=false npm run build --prefix client"

runConfig:
  cpu: 1
  memoryMiB: 1024

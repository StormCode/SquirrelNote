kind: "AppHostingYaml"
schemaVersion: "v1"

buildConfig:
  buildCommand: |
    npm install
    npm install --prefix client
    cd client && CI=false npm run build
    cd ..
    # 將產出搬移到一個絕對不會被誤刪的名字
    cp -r client/build ./dist_final

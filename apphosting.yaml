kind: "AppHostingYaml"
schemaVersion: "v1"

buildConfig:
  # 1. 強制進入 client 並執行 build
  # 2. 為了防止路徑迷失，build 完後立刻把內容搬移到根目錄的一個新資料夾叫 public_html
  buildCommand: "npm install && npm install --prefix client && npm run build --prefix client && ls -R client"
